public enum PlayerState
{
    Alive,
    Waiting,
    Choke,
    Dead,
}

public enum PlayerPosition
{
    Left,
    Right
}

public enum PlayerDirection
{
    Left, 
    Right
}

public enum LevelState
{
    Prepare,
    Playing,
    Waiting,
    Ending
}